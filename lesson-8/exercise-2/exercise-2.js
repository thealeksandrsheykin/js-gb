#!/usr/bin/env node

/*
Не выполняя код, ответить, что выведет браузер и почему:
*/

//A.
// if (!("a" in window)) {
//     var a = 1;
// }
// alert(a);

// Выведет "undefined", так как переменная "a" не определена и условие не является True из-за этого переменной "a" не
// присваивается 1.